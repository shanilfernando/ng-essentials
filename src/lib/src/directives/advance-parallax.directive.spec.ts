import { AdvanceParallaxDirective } from './advance-parallax.directive';

describe('AdvanceParallaxDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvanceParallaxDirective();
    expect(directive).toBeTruthy();
  });
});
