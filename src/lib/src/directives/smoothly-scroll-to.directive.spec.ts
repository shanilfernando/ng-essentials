import { SmoothlyScrollToDirective } from './smoothly-scroll-to.directive';

describe('SmoothlyScrollToDirective', () => {
  it('should create an instance', () => {
    const directive = new SmoothlyScrollToDirective();
    expect(directive).toBeTruthy();
  });
});
